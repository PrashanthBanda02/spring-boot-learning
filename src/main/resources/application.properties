spring.application.name=demoproject

spring.datasource.url=jdbc:postgresql://localhost:5432/spring-learning
spring.datasource.username=postgres
spring.datasource.password=rgukt
spring.datasource.driver-class-name=org.postgresql.Driver

# Optional: JPA configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.sql.init.mode = never

# To send any detailed error messages back to the client (for security reasons it won't send by default).
server.error.include-message = always
